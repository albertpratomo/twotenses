---
import formatLink from '@/utils/formatLink';

interface Props {
    class: any;
    label?: string;
    link: {
        cached_url: string;
        target?: string;
        title?: string;
    };
}

let {class: classes, label, link} = Astro.props;

label = label || link.title;

const {href, target} = formatLink(link);
---

<a
    class:list={classes}
    href={href}
    target={target}
>
    {label}

    <slot></slot>
</a>
